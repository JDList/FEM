A set of programs to run a dirichlet Finite Element Model on a set of nodes and elements from first principles.
Run through the commands:
g++ FEMlib.cpp FEMtest.cpp -o test
then run the test program
then run the FEMdisp.py program to display the results.
